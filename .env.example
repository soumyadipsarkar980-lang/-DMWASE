# Optional deployment token used by the Vercel CLI.
# Create one in Vercel: Account Settings > Tokens, then run:
# VERCEL_TOKEN=your_token npm run deploy:vercel -- --token "$VERCEL_TOKEN"
VERCEL_TOKEN=
